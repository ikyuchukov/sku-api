[project]
name = "app"
version = "0.1.0"
description = "API for SKU management"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "sqlalchemy>=2.0.49",
]
